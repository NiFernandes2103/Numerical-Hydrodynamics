
void print_mtrix( vector<vector<>>)
int rows = qprime.get(0).size();
        int cols = qprime.get(0)[0].size();
        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (j == cols - 1) {
                    cout << qprime.get(6)[i][j] << "; t:" << t <<endl;
                } else {
                    cout << qprime.get(6)[i][j] << ',';
                }
            }
        }